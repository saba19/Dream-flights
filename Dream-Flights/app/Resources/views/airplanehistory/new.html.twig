{% extends 'base.html.twig' %}

{% block content %}
    <br>
    <h2> Create new airplane history</h2>
    <br>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('airplanehistory_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
